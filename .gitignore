node_modules
.env

# Dev overrides for local testing (Server SDK Bootstrap Template)
# Contains local config values before deploying to Secret Manager
dev-overrides.json

# MCP workspace configuration (credentials via gcloud ADC)
.descix-workspace.json
.descix-setup-guide.md
.descix.ignore

# Hardhat files
/cache
/artifacts
/.deps

# TypeChain files
/typechain
/typechain-types

# Solidity-coverage files
/coverage
/coverage.json

# Other ignored folders
**/roomsite/
**/temp
**/dist
/private
./**/npm/
.DiscordSampleActivity
/.venv

# Ignore build directories
**/build

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
secret
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# LaTeX build intermediaries
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.lof
*.lot
*.nav
*.out
*.run.xml
*.snm
*.synctex.gz
*.toc
*.vrb

# DeSciX
.descix/wallet.json
*.secret.json
# ── Editor / tool transient backups (never tracked) ──
*.bak-*

# ── Claude-Code session artifacts (not part of the package) ──
.claude/

# ── npm pack output (regenerated, not tracked) ──
*.tgz
descix-sdk/*.tgz
